import React, { FC } from "react";

const SortMenu: FC = () => {
  return (
    <div className="sort-menu">
      <h2 className="sort-menu__title">Порядок сортировки</h2>
      <div className="sort-menu__list">
      </div>
    </div>
  )
}

export default SortMenu;